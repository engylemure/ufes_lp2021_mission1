import { Color } from '@/utils/color'

export const Settings = Object.freeze({
    grid: {
        dimension: 6,
        nodeSize: 100,
        nodeOffset: 10,
        color: new Color(245, 245, 245, 1),
    },
})
